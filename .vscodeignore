# Ship only: dist/**, media/**, README.md, CHANGELOG.md, LICENSE, package.json.
# Everything else in this monorepo (site, backend, admin, docs, sources) is excluded.
.vscode/**
.vscode-test/**
.github/**
# Local agent/tooling config. Ignored by git, but vsce packages the working
# directory rather than the index, so it needs excluding here too.
.claude/**
src/**
web/**
admin/**
backend/**
supabase/**
dev/**
docs/**
design-mockups/**
scripts/**
videos/**
node_modules/**
**/.env
**/*.ts
**/*.map
esbuild.js
tsconfig.json
.eslintrc.json
.gitignore
.vscodeignore
.editorconfig
package-lock.json
*.vsix
*.zip
extension.vsixmanifest
SECURITY.md
DEPLOYMENT.md
codeslot_v1.md
**/.DS_Store
